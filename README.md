
  # Generate Responsive HTML CSS

  This is a code bundle for Generate Responsive HTML CSS. The original project is available at https://www.figma.com/design/vqo6OuHPCYFvMnbm5PGUoD/Generate-Responsive-HTML-CSS.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  